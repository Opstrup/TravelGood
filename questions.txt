Questions for the web services project:

1.  How should we simulate the error in the cancel booking? - Just hardcode it
    (should we make set a flag with the function call that makes it to fail or should we make it fail at random?)

2.  Are flights requried to "match" : date_for_travel < date_for_hometrip? - No
3.  The airline and hotel services are only SOAP? (Simpel SOAP and BEPEL), what are the requirements? - Just simpel SOAP
